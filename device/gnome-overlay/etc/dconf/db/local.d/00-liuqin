[org/gnome/desktop/interface]
# The default UI scale is 150%, set by the system monitor configuration
# /etc/xdg/monitors.xml (a per-monitor fractional scale; the integer-only
# scaling-factor key below cannot express 1.5).  This key is only the fallback
# for Mutter's *automatic* scale path (used if the saved monitor config does
# not match the connected panel); 2 is the nearest usable integer to 150% on
# this 1800x2880 11" panel, where 1x would be unreadable.
scaling-factor=uint32 2
# Show the battery percentage in the top bar. Upstream defaults this to
# false; on a tablet the charge level is a glanceable state, not a detail
# that needs the quick settings panel.
show-battery-percentage=true

[org/gnome/desktop/a11y/applications]
# No on-screen keyboard by default (user preference); a physical magnetic
# keyboard is the normal input, so it is only enabled per-session if needed.
screen-keyboard-enabled=false

[org/gnome/desktop/session]
# Five minutes. This was 0 -- never blank -- while the display was still being
# brought up, because a panel that blanks on its own is indistinguishable from
# one that has failed, and this project had already spent a session on that
# confusion. The display is settled now, and "never blank" is no longer free:
# on battery it is the single most expensive default the desktop has, and the
# reason it was set no longer exists.
idle-delay=uint32 300

[org/gnome/settings-daemon/plugins/power]
# GNOME cannot assign different actions to short and long KEY_POWER presses.
# liuqin-power-keyd owns that duration policy, while deliberately leaving the
# evdev stream visible to Mutter so a press still wakes the panel. Prevent GSD
# from opening or accidentally confirming its own shutdown dialog in parallel.
power-button-action='nothing'

# These were inert while idle-delay was 0 and are load-bearing now that it is
# 300: idle does reach the power plugin, and 'nothing' is what keeps it at
# blanking the screen instead of going on to suspend a board whose resume has
# never been tested. Raise them to 'suspend' in the same change that proves
# suspend, not before -- blanking on battery is the win here, and suspending is
# the risk.
sleep-inactive-ac-type='nothing'
sleep-inactive-battery-type='nothing'

[org/gnome/desktop/screensaver]
# A short power-key press explicitly locks even on the current development
# account. With its empty password this is a screen shield, not an authentication
# boundary; setting a real password remains a separate product gate.
lock-enabled=true
