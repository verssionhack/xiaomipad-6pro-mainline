<?xml version="1.0" encoding="UTF-8"?>
<!--
  liuqin (Xiaomi Pad 6 Pro) default display scale: 150%.

  GNOME expresses a per-monitor fractional scale through the saved monitor
  configuration (Mutter's monitors.xml, format version 2), NOT through the
  integer-only org.gnome.desktop.interface.scaling-factor key.  This file is a
  system-wide default (XDG_CONFIG_DIRS -> /etc/xdg), so it applies to every
  login (the tablet runs as root) without depending on a per-user home, and it
  is not clobbered when the session auto-saves a user config.

  It targets the built-in panel: a dual-DSI Novatek NT36532 with no EDID, so
  Mutter reports connector "DSI-1" with vendor/product/serial "unknown", and a
  single native mode 1800x2880 @ 120 Hz (pixel clock 785245 kHz, vrefresh
  119.9999 which round-trips as 120.000).  scale 1.5 -> logical 1200x1920.

  If the connector or mode ever changes (e.g. a panel swap), the saved config
  simply stops matching and GNOME falls back to its automatic scale.
-->
<monitors version="2">
  <configuration>
    <layoutmode>logical</layoutmode>
    <logicalmonitor>
      <x>0</x>
      <y>0</y>
      <scale>1.5</scale>
      <primary>yes</primary>
      <transform>
        <rotation>normal</rotation>
        <flipped>no</flipped>
      </transform>
      <monitor>
        <monitorspec>
          <connector>DSI-1</connector>
          <vendor>unknown</vendor>
          <product>unknown</product>
          <serial>unknown</serial>
        </monitorspec>
        <mode>
          <width>1800</width>
          <height>2880</height>
          <rate>120.000</rate>
        </mode>
      </monitor>
    </logicalmonitor>
  </configuration>
</monitors>
